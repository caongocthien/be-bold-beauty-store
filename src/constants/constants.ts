const CONSTANTS = {
  allProducts: 'Tất cả sản phẩm',
  queryParam: {
    GET_PRODUCTS_BY_BRAND: 'filters[bb_brand][id][$eq]',
    GET_PRODUCTS_BY_CATEGORY: 'filters[bb_product_category][id][$eq]'
  }
}

export default CONSTANTS
